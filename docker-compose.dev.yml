services:
  fritzbox-mcp-http:
    build:
      context: .
      dockerfile: Containerfile
    command: ["--transport", "streamable-http", "--port", "${MCP_PORT:-8080}", "--host", "0.0.0.0", "--debug"]
    environment:
      - FRITZ_TLS=false
